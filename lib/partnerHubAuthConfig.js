// Partner Hub codes are generated through Supabase's magic-link flow, even
// though COP delivers the raw OTP in its own email. Generation and
// verification must use the same Auth type.
export const PARTNER_HUB_OTP_TYPE = 'magiclink';
